# packages/shared

Utils, formatters, date logic dùng chung. Sẽ chứa bản port của thuật toán Smart Score
(`SmartScore` / `Risk` / `RecommendedAction`) tham chiếu từ
`apps/google-sheets/src/05_SmartEngine.gs` — giữ cùng bộ quy tắc, không bịa logic mới.
Chưa có code — khởi tạo khi cần ở Phase 09+ (UI cần hiển thị Smart fields từ mock trước Phase 29).
